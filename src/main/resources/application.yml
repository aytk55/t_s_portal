
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/s1
    username: postgres
    password: 123456123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true